## 1. 공통 테마 시스템 (app.js)

- [x] 1.1 `initTheme()` 함수 — localStorage → 없으면 prefers-color-scheme → html[data-theme] 설정
- [x] 1.2 `toggleTheme()` 함수 — 현재 테마 반전 후 localStorage 저장, 버튼 아이콘 갱신
- [x] 1.3 CSS 변수 블록 — 라이트/다크 공통 `<style>` 스니펫 (각 HTML에 붙여넣기용)

## 2. 로그인 / 가입 화면

- [x] 2.1 `login.html` — CSS 변수 적용, 우측 상단 플로팅 토글 버튼, 카드/입력창 변수화
- [x] 2.2 `signup.html` — 동일

## 3. 팀 선택 화면

- [x] 3.1 `team.html` — CSS 변수 적용, 헤더 토글 버튼 추가, 카드/배너 변수화

## 4. 칸반 화면

- [x] 4.1 `kanban.html` — CSS 변수 적용, 헤더 토글 버튼 추가
- [x] 4.2 컬럼(TODO/DOING/DONE) 배경 — 다크모드에서 어두운 색조 유지
- [x] 4.3 카드 배경/텍스트/테두리 변수화
- [x] 4.4 모달 배경/입력창/버튼 변수화

## 5. 채팅 화면

- [x] 5.1 `chat.html` — CSS 변수 적용, 헤더 토글 버튼 추가
- [x] 5.2 말풍선 — 본인(teal 계열 유지) / 타인(다크모드에서 --surface2) 변수화
- [x] 5.3 입력창/전송 버튼 변수화

## 6. 멤버 화면

- [x] 6.1 `members.html` — CSS 변수 적용, 헤더 토글 버튼 추가, 목록 항목 변수화

## 7. 검증

- [x] 7.1 6개 화면 모두 토글 동작 확인
- [x] 7.2 새로고침 후 테마 유지 확인
- [x] 7.3 시스템 다크모드 설정 시 초기 테마 확인
